{
  "name": "healthher",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm --prefix api install && npm --prefix web install",
    "dev": "concurrently -n api,web -c magenta,cyan \"npm --prefix api run dev\" \"npm --prefix web run dev\"",
    "test": "npm --prefix api test"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
