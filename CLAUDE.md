# CLAUDE.md

## Project
This is a website builder for calorie tracking.

## Rules
- Never add new things (files, folders, features, pages, sections). Only update/modify existing things.
- If a change seems to require something new, ask the user first instead of creating it.
